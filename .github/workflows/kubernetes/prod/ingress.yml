apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: communicator-users-service
  namespace: communicator-prod
  annotations:
    konghq.com/protocols: https
    konghq.com/https-redirect-status-code: '308'
    konghq.com/plugins: communicator-prod-auth0-jwt,communicator-prod-cors
spec:
  tls:
    - hosts:
        - prod.users-service.communicator.kamilwronka.com
      secretName: communicator-prod-tls
  ingressClassName: kong
  rules:
    - host: prod.users-service.communicator.kamilwronka.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: communicator-users-service
                port:
                  number: 4000
