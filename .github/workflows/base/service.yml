apiVersion: v1
kind: Service
metadata:
  name: communicator-users-service
  labels:
    app: communicator-users-service
    service: communicator-users-service
  annotations:
    ingress.kubernetes.io/service-upstream: "true"
spec:
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: communicator-users-service
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: communicator-users-service
  labels:
    account: communicator-users-service
